#include <stdio.h>
#include <string>
#include <iostream>

using namespace std;

int main()
{
    string s;
    cin >> s;

    string s2 = s.at(s.length() - 1);

    for (int base_i = s.length() - 1; base_i > 0; --base_i)
    {
    }
    return 0;
}